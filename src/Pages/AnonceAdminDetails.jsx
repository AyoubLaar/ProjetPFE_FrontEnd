import React from "react";
import { useParams } from "react-router";

const AnonceAdminDetails = () => {
  const idAnonce = React.useRef(useParams().id);
  return;
};

export default AnonceAdminDetails;
